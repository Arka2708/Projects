<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Data</title>
    <style>
        body {
            background-color: #f2f2f2; /* Light grey background */
            font-family: Arial, sans-serif; /* Set font */
            margin: 0;
            padding: 0;
        }
        h1 {
            color: #333; /* Dark text color */
            text-align: center;
            margin-bottom: 30px;
        }
        form {
            width: 300px;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff; /* White background */
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Add shadow */
        }
        label {
            color: #555; /* Dark grey label text color */
            font-weight: bold; /* Bold label text */
            display: block;
            margin-bottom: 5px;
        }
        textarea,
        input[type="file"],
        input[type="submit"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc; /* Light grey border */
            border-radius: 5px;
            box-sizing: border-box;
            font-size: 16px; /* Increase font size */
        }
        input[type="file"] {
            margin-bottom: 20px;
        }
        input[type="submit"] {
            background-color: #007bff; /* Blue button background */
            color: #fff; /* White button text color */
            cursor: pointer;
            transition: background-color 0.3s ease; /* Smooth transition */
        }
        input[type="submit"]:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }
        a {
            color: #007bff; /* Blue link text color */
            text-decoration: none;
            display: block;
            text-align: center;
            font-size: 14px; /* Decrease font size */
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>Hi <%= user %>!!!</h1>
    <h1>Upload Text Data and File</h1>
    <form action="/<%= user %>/upload" method="post" enctype="multipart/form-data">
        <label for="data">Text Data:</label><br>
        <textarea name="data" id="data" rows="4" cols="50"></textarea><br><br>
        <label for="file">File:</label><br>
        <input type="file" name="file" id="file"><br><br>
        <input type="submit" value="Upload">
        <br>
        <br>
        <a href="/<%= user %>/collection">Show my repo</a>
    </form>
</body>
</html>
